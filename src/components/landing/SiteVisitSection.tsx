import SiteVisitForm from './SiteVisitForm';

const SiteVisitSection = () => {
  return (
    <section className="bg-background">
      <SiteVisitForm source="Site Visit Section" variant="horizontal" />
    </section>
  );
};

export default SiteVisitSection;
